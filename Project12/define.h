#include <iostream>
#include <Windows.h>
#include <cmath>

using namespace std;

#define MIN(a, b) (a < b ? a : b)
#define MAX(a, b) (a > b ? a : b)
#define SQUARE(a) (a * a)
#define DEGREE(a, b) (pow(a, b))
#define EVENA(a) (a % 2 == 0 ? true : false)
#define EVENB(b) (b % 2 == 0 ? true : false)
